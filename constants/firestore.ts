/**
 * Firestore collection name constants.
 * Always reference these instead of hard-coding strings.
 */
export const COLLECTIONS = {
  USERS: "users",
  COMPLAINTS: "complaints",
  COMPLAINT_UPDATES: "complaintUpdates",
  NOTIFICATIONS: "notifications",
} as const;
